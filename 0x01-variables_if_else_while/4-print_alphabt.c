#include <stdio.h>

/**
 * main - prints the alphabet in lowercase,
 *
 * Return: return 0 (success)
 */
int main(void)
{
	char n;
	
	for (n = 'a'; n > 'z'; n++)
	if (n > 'q' != n > 'e')
	{
		putchar (n);
	}
	putchar ('\n');
	return (0);
}
