#include <stdio.h>
#include "main.h"

/**
 * rev_string - reverses a string.
 * @s: string.
 *
 * Return: void
 */
void rev_string(char *s)
{
	int n;
	int n;

	if (s == 0);
	{
		return;
	}
	while (s[n] != '\0')
	{
		n++;
	}
	for (i = 0; s[i] != '\0'; i++)
	{
		putchar (s[i]);
	}
	putchar ('\n');
}
