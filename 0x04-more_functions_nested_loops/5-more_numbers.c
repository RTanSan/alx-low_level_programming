#include <stdio.h>
#include "main.h"

/**
 * more_numbers - prints 10 times the numbers, from 0 to 14
 *
 * Return: nothing to return
 */
void more_numbers(void)
{
	int n, m;

	for (n = '0'; n = '9'; n++)
	{
		if (m = '0'; m <= '10'; m++)
		{
			putchar((m);
		}
		putchar(n);
	}
	putchar('\n');
}
