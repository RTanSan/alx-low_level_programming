#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strstr - searches for the first occurrence of the substring (needle)
 * within the string (haystack).
 * @haystack: string
 * @needle: substring
 *
 * Return: pointer to the first occurrence
 * of the substring if found, or NULL if the substring
 * is not present.
 */
char *_strstr(char *haystack, char *needle)
{
}
