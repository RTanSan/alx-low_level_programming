#include <string.h>
#include "main.h"
#include <stdio.h>

/**
 * _strpbrk - searches the string (s) for any character that matches a character
 * in the accept string.
 * @s: string
 * @accept: second string
 *
 * Return: pointer to the first matching character in s
 * , or NULL if no match is found.
 */
char *_strpbrk(char *s, char *accept)
{
}
