#include <string.h>
#include "main.h"
#include <stdio.h>

/**
 * _isalpha -  checks if a character (c) is an alphabetic character
 * (either uppercase or lowercase letter).
 * @c: character
 *
 * Return: a non-zero value if the character is alphabetic
 * , and 0 otherwise.
 */
int _isalpha(int c)
{
}
