#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - Calculates the length of the string s by counting
 * the number of characters until the
 * null terminator ('\0') is encountered.
 * @s: string
 *
 * Return: length of the string.
 */
int _strlen(char *s)
{
}
