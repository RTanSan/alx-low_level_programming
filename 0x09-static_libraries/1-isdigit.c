#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * int _isdigit(int c) - Checks if the given character c is a digit
 * @c: the number to check
 *
 * Return: returns a non-zero value if c,
 *  is a digit, and 0 otherwise.
 */
int _isdigit(int c)
{
}
